fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
langchain>=0.3.0
langgraph>=0.2.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
chromadb>=1.5.9
# Durable chat session store (V3 Step 0: services/chat/chat_service.py) via
# SQLAlchemy Core. One query path serves SQLite (local) and Postgres (prod); the
# URL selects the dialect. psycopg (v3) is the Postgres driver used only when
# SESSION_DB_URL points at postgresql+psycopg://... — SQLite needs no driver.
sqlalchemy>=2.0.0,<3.0.0
psycopg[binary]>=3.1.0,<4.0.0
openai>=1.50.0
anthropic>=0.34.0
requests>=2.32.0
python-dotenv>=1.0.0
# Inbound per-IP rate limiting on the paid endpoints (api/limiter.py).
slowapi>=0.1.9
# Outbound HTTP retry/backoff for the services layer (services/http_retry.py).
# Already pulled in transitively by langchain; pinned here as a direct dep.
tenacity>=8.2.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
