"""V3 trip-planner agent package.

Sibling of ``agent/workflow/``. Houses the multi-day / multi-stop onsen
trip-planner (the project's first true agent — dynamic tool sequencing +
re-planning). PR2 ships only the dead stub in ``agent.py``; the real slot-filling
agent and its tools land in later PRs (see docs/v3-trip-planner-plan.md §6).
"""
